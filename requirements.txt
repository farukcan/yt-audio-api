# requirements.txt
# Dependencies for the YouTube Audio Converter API

Flask>=3.0,<4.0
yt-dlp>=2024.1.1

# Transcription pipeline (Apple Silicon)
demucs           # vocal / instrumental separation
mlx-whisper      # Whisper on Apple MLX (Neural Engine)
torchcodec       # audio encoding backend required by demucs/torchaudio

# System requirement (install manually if missing)
# FFmpeg must be installed separately for audio conversion
# On macOS:    brew install ffmpeg
# On Ubuntu:   sudo apt install ffmpeg
